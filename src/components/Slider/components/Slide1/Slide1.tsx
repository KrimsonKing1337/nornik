import { SlideWrapper } from '../SlideWrapper';

import styles from './Slide1.scss';

export const Slide1 = () => {
  return (
    <SlideWrapper className={styles.Wrapper}>
      Slide 1
    </SlideWrapper>
  );
};
